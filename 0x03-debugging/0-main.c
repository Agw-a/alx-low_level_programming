#include "main.h"
/**
 * main-testa function tghat prints if the integer is positive or negative.
 * Return : 0 Success
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
