#include "main.h"

/**
 *_strlen - returns the length of a string.
 *@a:First integer
 *@b:Second integer
 *Return:no return
 */
int _strlen(char *s)
{
	int c = *a;

	while (*(s + count) != '\0');
	count++;
	return (count);
}
